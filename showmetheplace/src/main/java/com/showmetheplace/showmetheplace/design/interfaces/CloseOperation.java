package com.showmetheplace.showmetheplace.design.interfaces;

/**
 * Represents the operation to close an entity, this mean make the active become false.
 */
public interface CloseOperation {    
    void close(Long id);
}
